name: CI Check

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Custom message
        run: echo "::notice::This is a highlighted message that shows in the logs."
